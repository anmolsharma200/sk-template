plugins {
    id("io.micronaut.platform.catalog") version "4.6.1" apply false
}

subprojects {
    group = "<%= appPackage %>"
    version = "1.0.0"

    repositories {
        mavenCentral()
    }
}
